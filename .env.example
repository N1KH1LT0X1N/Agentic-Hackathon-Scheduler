# Database Configuration
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hackathon_copilot"

# LLM API Configuration
# Supports Anthropic Claude (sk-ant-...) or OpenAI (sk-...)
# Leave as "your-llm-key" to use stub mode for development
LLM_API_KEY="your-llm-key"

# Session Secret
# Generate a strong random secret for production
# Example: openssl rand -base64 32
SESSION_SECRET="change-me-to-a-random-secret"

# Node Environment
# Options: development, production
NODE_ENV="development"

# Application Configuration
# Next.js server port (default: 3000)
# PORT=3000

# Optional: CORS Configuration
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS="http://localhost:3000,https://yourdomain.com"
